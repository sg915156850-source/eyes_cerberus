# Eyes Cerberus

# Runtime state: events, evidence, quarantine, baselines, markers, logs.
# Never commit quarantined samples or forensic captures.
state/

# Local config (may contain a Telegram token / webhook URL).
etc/cerberus.env

# Build output (make deb).
dist/

# Legacy runtime artefacts (pre-2.0 layout), in case an old checkout is around.
*.pid
*.out
hunt.log
defense/
m/
honeypot/decoys/
honeypot/logs/
/var/malware_containment/

# Editor / OS
*.swp
*~
*.orig
*.rej
.DS_Store
.vscode/
.idea/
