# System Eyes configuration - sourced by watcher.sh
# SENSOR_INTERVAL: seconds between sensor polls (default 15)
# ANALYST_DURATION: seconds to run analyst collection (default 10)
# ANALYST_SNAPSHOT: seconds between analyst snapshots (default 2)
# DEEP_LOG: true/false to enable deeper artifacts (default true)

SENSOR_INTERVAL=10
ANALYST_DURATION=30
ANALYST_SNAPSHOT=3
DEEP_LOG=true
